
import React, { Fragment } from 'react';
import ActivityLinks from '../containers/activity/ActivityLinks';

const Activities = () => {
    return (
        <Fragment>
            <ActivityLinks/>
        </Fragment>
    );
};

export default Activities;